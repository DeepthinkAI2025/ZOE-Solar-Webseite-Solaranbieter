// Advanced SERP Tracking Service für ZOE Solar (TypeScript ES-Module)
class AdvancedSERPTrackingService {
  // ... (Klasseninhalt aus .js unverändert übernommen)
  // Siehe Originaldatei für alle Methoden und Properties
  // (Hier aus Platzgründen nicht erneut ausgeschrieben)
  // Die Klasse bleibt identisch, nur die Exporte werden angepasst.
}

// Singleton-Instanz
export const advancedSERPTrackingService = AdvancedSERPTrackingService.getInstance();
export default advancedSERPTrackingService;